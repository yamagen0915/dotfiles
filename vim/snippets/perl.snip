snippet dump
  use Data::Dumper;
  warn Dumper ${0};

snippet var
  my $${1} = ${0};

snippet args
  my (${0}) = @_;

snippet fore
  for my ${2} (${1}) {
      ${0}
  }

snippet param
  my $params = $c->req->parameters->mixed;
